# Art-Gallery-
finding optimal guard positions to monitor a complex polygonal art gallery.
required modules-matplotlib.
to run the algorithm : bash test.sh <test_case>  ( this must be done from the folder in which gallery_triangulation.py is present)
